# 项目规则 - AI设计作品集展示平台

## 一、技术栈规范

| 分类 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue 3 | 3.4.x |
| 构建工具 | Vite | 6.5.x |
| 样式框架 | TailwindCSS | 3.4.x |
| 图标库 | Lucide Vue Next | 1.6.x |

## 二、核心模块

| 模块 | 功能描述 |
|------|----------|
| ProjectTabs | 项目标签切换 |
| FolderIndex | 分类目录展示（支持嵌套） |
| ImageGallery | 图片浏览（缩放、拖动、切换） |
| UnlockModal | 授权解锁弹窗 |
| scanner.ts | 文件系统动态扫描 |

## 三、功能清单

| 功能 | 优先级 | 状态 |
|------|--------|------|
| Project切换 | 高 | ✅ |
| 文件夹索引 | 高 | ✅ |
| 图片展示 | 高 | ✅ |
| 左右切换 | 高 | ✅ |
| 图片翻转（双击查看Prompt） | 高 | ✅ |
| 授权解锁 | 高 | ✅ |
| 一键复制 | 中 | ✅ |
| 图片缩放 | 中 | ✅ |
| 动态扫描 | 中 | ✅ |
| 响应式设计 | 中 | ✅ |
| GIF支持 | 低 | ✅ |

## 四、开发最佳实践

### 4.1 路径处理
- 始终使用完整路径
- 中文路径需要 URL 编码
- 避免硬编码路径

### 4.2 布局设计
- 使用 `max-width`/`max-height` 约束图片
- 容器高度使用 `calc()` 动态计算
- 考虑响应式断点

### 4.3 交互逻辑
- 使用计数器+定时器实现双击检测
- 拖动时记录相对位置
- 触摸事件需要区分单指和双指

### 4.4 数据加载
- 使用 API 动态获取数据
- 支持多种文件格式
- 不要依赖固定文件名

### 4.5 移动端适配
- 使用响应式设计
- 实现触摸手势支持
- 移动端使用抽屉式菜单

### 4.6 部署与环境
- 配置正确的 Vite `base` 路径
- 将动态数据静态化（预生成 JSON）
- 使用环境变量适配不同环境
- 中文路径需要 URL 编码
- 部署前验证构建是否成功

## 五、问题解决流程

```
用户反馈问题
    │
    ▼
确认问题现象
    │
    ▼
定位问题位置（组件/工具函数/配置）
    │
    ▼
分析根本原因
    │
    ▼
制定解决方案
    │
    ▼
实施修复
    │
    ▼
验证修复效果
    │
    ▼
记录问题与解决方案
```

## 六、常见问题速查

| 问题 | 解决方案 |
|------|----------|
| 图片无法显示 | 检查路径是否包含项目ID前缀 |
| Prompt加载失败 | 检查中文路径是否URL编码 |
| 图片被裁剪 | 使用 `object-fit: contain` 和 `overflow: auto` |
| 双击无反应 | 检查触摸事件与双击事件是否冲突 |
| 动态扫描不生效 | 确保调用 `/api/scan` API |

## 七、优化建议

### 性能优化
- 图片懒加载
- 图片压缩处理
- 缓存机制优化

### 功能扩展
- 图片批量上传
- 图片标注功能
- 评论系统
- 分享功能

### 安全增强
- 密码复杂度验证
- 会话超时机制
- 访问日志记录

## 八、文件格式支持

### 图片格式
- jpg, jpeg
- png
- gif（自动播放）
- webp
- svg

### 视频格式
- mp4, webm, ogg, mkv, mov

### 说明文件格式
- .txt
- .md
